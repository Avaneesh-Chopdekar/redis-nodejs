{
  "name": "redis-clone",
  "version": "1.0.0",
  "main": "src/server.js",
  "type": "module",
  "bin": "./src/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "description": "Redis clone in Node.js",
  "dependencies": {
    "dotenv": "^16.5.0"
  }
}
